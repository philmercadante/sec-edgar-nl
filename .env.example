# Required: SEC EDGAR requires a User-Agent header with contact info
# Format: "CompanyName AdminEmail" (e.g., "MyApp admin@example.com")
SEC_USER_AGENT=sec-edgar-nl your-email@example.com

# Optional: Anthropic API key for natural language query parsing (Phase 2)
# ANTHROPIC_API_KEY=sk-ant-...
